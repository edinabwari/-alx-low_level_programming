#include "main.h"
/**
 * main -  prints the string "_putchar"followed by a new line from a character array
 *
 * Return: Always 0.
 */
int main(void)
{
	char c[]={_putchar}
		int i;

	for(i=0;i<8;i++)
	{
		putchar(c[]);
		putchar("\n");
	}

	return (0);
}
